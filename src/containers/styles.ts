import { Theme } from '@mui/material'

const ideaPlatformStyle = (theme: Theme) => ({
    logo: {
        margin: theme.spacing(6, 0, 6,0),
        borderRadius: '50%',
        width: '90px',
    },
})

export default ideaPlatformStyle
